syntax = "proto3";

package e2ees;

option java_package = "org.e2eelab.proto.e2ees";
option java_outer_classname = "RegisterUserRequestProto";
option objc_class_prefix = "Objc";
option swift_prefix="Swift";

import "e2ees/ExtraPreKeyPublic.proto";
import "e2ees/IdentityKeyPublic.proto";
import "e2ees/OneTimePreKeyPublic.proto";
import "e2ees/SignedPreKeyPublic.proto";

/**
 * Request for registering an account.
 */
message RegisterUserRequest {
  uint32 e2ees_pack_id = 1;                                   // The e2ees pack ID.
  string user_name = 2;                                       // The user's name.
  string user_id = 3;                                         // The user's ID.
  string device_id = 4;                                       // The ID of the user's device.
  string authenticator = 5;                                   // The email (mailto:xxx@xxx) bound to the registering user.
  string auth_code = 6;                                       // A 6-digit number received by email, or an invitation in the format (e2ee:invitorUserId@invitorDomain;invite=xxxxx).

  IdentityKeyPublic identity_key_public = 11;                 // The public part of the identity key pair.
  SignedPreKeyPublic signed_pre_key_public = 12;              // The public part of the signed pre-key pair.
  repeated OneTimePreKeyPublic one_time_pre_key_list = 13;    // The public parts of all the user's one-time pre-key pairs.
  repeated ExtraPreKeyPublic extra_pre_key_list = 14;         // The public parts of all the user's optional extra pre-keys.
}
